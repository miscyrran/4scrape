{
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git commit -m 'Add archived threads section with manual and auto-archive *)",
      "Bash(git push *)",
      "Bash(git commit -m 'Update README to document all changes since initial commit *)"
    ]
  }
}
